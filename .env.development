# Development Environment Variables
# API URL for local development with direct backend connection
VITE_API_URL=http://localhost:8000/api

# Optional: Enable UUID processing IDs (default uses existing format)
# VITE_USE_UUID_PROCESSING_IDS=true